[project]
name = "LitFlow"
version = "0.1.0"
requires-python = "==3.12.*"

dependencies = [
  "flask",
  "authlib",
  "celery",
  "redis",
  "python-dotenv",
  "surya-ocr==0.6.13",
  "pdf2image",
  "gunicorn",
  "requests",
]
